extends ../layout.pug
block content
  .container
    .row
      each item in movies
        .col-12.col-sm-6.col-md-3.col-lg-2
          a(href=`/movie/${item._id}`)
            img.card-img-top.w-100(src=`${item.poster}`, alt=`${item.title}`)
          .caption.text-center
            h5 #{title}
            p: a.btn.btn-primary(href=`/movie/${item._id}`, role="button")
              | 观看预告片